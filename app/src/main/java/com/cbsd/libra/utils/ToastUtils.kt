package com.cbsd.libra.utils

import android.widget.Toast
import com.cbsd.libra.app.LibraApplication

object ToastUtils {

    fun show(message: String){
//        Toast.makeText(LibraApplication.instance!!.applicationContext, message, Toast.LENGTH_SHORT).show()
    }
}